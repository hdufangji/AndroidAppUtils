<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="Theme">
        <item name="android:windowNoTitle">false</item>
    </style>
    
    <style name="Widget" parent="@android:style/Widget">
    </style>
    
    <style name="TitleBar" parent="@style/Widget">
        <item name="android:textColor">@color/title_text</item>
    </style>
    <style name="TitleBar.Container">
        <item name="android:id">@id/title_container</item>
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:orientation">horizontal</item>
    </style>
    <style name="TitleBar.Text">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">left</item>
        <item name="android:textColor">@color/title_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/titlebar_textSize</item>
    </style>
    <style name="TitleBar.Icon">
        <item name="android:layout_width">@dimen/titlebar_icon_size</item>
        <item name="android:layout_height">@dimen/titlebar_icon_size</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_marginLeft">@dimen/titlebar_marginLeft</item>
        <item name="android:layout_marginRight">@dimen/titlebar_marginRight</item>
		<item name="android:scaleType">centerInside</item>
    </style>
    
    <style name="Content" parent="@style/Widget">
        <item name="android:textColor">@color/content_text</item>
        <!-- item name="android:background">@drawable/background_wave</item -->
    </style>
    
	<style name="Theme.Transparent" parent="android:Theme">
	    <item name="android:windowIsTranslucent">true</item>
	    <item name="android:windowBackground">@android:color/transparent</item>
	    <item name="android:windowContentOverlay">@null</item>
	    <item name="android:windowNoTitle">true</item>
	    <item name="android:windowIsFloating">true</item>
	    <item name="android:backgroundDimEnabled">false</item>
	</style>

</resources>
